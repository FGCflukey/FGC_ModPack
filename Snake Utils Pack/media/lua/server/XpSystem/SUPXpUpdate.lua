-- require "XpSystem/XpUpdate";

-- xpUpdate.suplevelPerk = function(owner, perk, level, addBuffer)
	-- ISSkillProgressBar.Timer = 1;
	-- --ISSkillProgressBar.Hour = math.floor(math.floor(GameTime:getInstance():getTimeOfDay() * 3600) / 3600);
-- end

-- Events.LevelPerk.Add(xpUpdate.suplevelPerk);